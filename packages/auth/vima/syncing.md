# Synchronizing to Database

Sync your role and permission definitions from `Setup.php` into your database:

```bash
php spark vima:sync
```

You can also manage roles and permissions directly via CLI. See [CLI Commands](/packages/auth/cli).
